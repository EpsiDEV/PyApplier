class Logger():
    #TODO
    """Logs emails to a Google Sheet with the following columns:
        - Company name
        - Date of the email
        - Domain of the company (fintech, web agency, etc.)
        - Email address
        - Google hyperlink to the search result
        (static columns:)
        - Company has replied (red color by default)
        - Interview scheduled (red color by default)
        - Second interview scheduled (red color by default)
        - Job offer (red color by default)
        """
    def __init__(self):
        pass